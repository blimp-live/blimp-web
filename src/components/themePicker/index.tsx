import * as React from 'react';
import {themesList} from '../../themes/themesList';

interface Props {
  themes: ,
}

export class ThemePicker extends React.Component<Props> {
  render() {

    return (


    );
  }
}

export default ThemePicker;
